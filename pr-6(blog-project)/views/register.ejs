<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - BlogCanvas</title>
    <style>
        *, *::before, *::after {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body, input {
            background: #333;
            color: #fff;
        }

        main {
            width: 100%;
            min-height: 100vh;
            background: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }

        .box {
            width: 500px;
            background: rgba(255, 255, 255, 0.3);
            padding: 3rem;
            backdrop-filter: blur(30px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            text-align: center;
        }

        .heading h2 {
            font-size: 3rem;
            font-weight: 700;
            color: #fff;
        }

        .input-wrap {
            position: relative;
            margin-bottom: 2rem;
        }

        .input-field {
            width: 100%;
            padding: 15px;
            background: rgba(255, 255, 255, 0.4);
            border: none;
            outline: none;
            border-radius: 10px;
            font-size: 1.2rem;
            color: #fff;
        }

        .sign-btn {
            width: 100%;
            background: #fff;
            color: #333;
            padding: 15px;
            border: none;
            border-radius: 15px;
            font-size: 1.2rem;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s ease;
        }

        .sign-btn:hover {
            background: #444;
            color: #fff;
        }

        .toggle {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            display: block;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <main>
        <div class="box">
            <div class="heading ">
                <h2>Register</h2>
            </div>
            <form action="/registerUser" method="post">
                <div class="input-wrap">
                    <input type="text" id="name" name="name" class="input-field" required placeholder="Enter Your Full Name" />
                </div>
                <div class="input-wrap">
                    <input type="email" id="email" name="email" class="input-field" required placeholder="Enter Your Email" />
                </div>
                <div class="input-wrap">
                    <input type="password" id="password" name="password" class="input-field" required placeholder="Create a Password" />
                </div>
                <input type="submit" value="Register" class="sign-btn" />
            </form>
            <a href="/" class="toggle">Already have an account? Login</a>
        </div>
    </main>
</body>
</html>
