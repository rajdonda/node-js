<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.movie_name %> Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            background-image: linear-gradient(to bottom, #f2f2f2, #e6e6e6); /* Changed background gradient */
            background-size: cover;
            background-position: center;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Poppins', sans-serif;
            color: #333; /* Changed text color */
        }

        .container {
            max-width: 1200px; /* Adjusted max-width */
            padding: 60px; /* Adjusted padding */
            background-color: rgba(255, 255, 255, 0.8); /* Adjusted background color */
            border-radius: 20px; /* Adjusted border-radius */
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2); /* Adjusted box-shadow */
        }

        .row {
            display: flex;
            align-items: center;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.95); /* Adjusted background color */
            border-radius: 20px; /* Adjusted border-radius */
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2); /* Adjusted box-shadow */
            padding: 40px; /* Adjusted padding */
            width: 40%; /* Adjusted width */
            margin: 15px; /* Adjusted margin */
        }

        .card h1 {
            font-size: 3.5rem; /* Adjusted font size */
            font-weight: 600;
            color: #333; /* Changed text color */
            text-shadow: 0 0 8px rgba(0, 0, 0, 0.2); /* Adjusted text shadow */
        }

        .card p {
            font-size: 1.8rem; /* Adjusted font size */
            color: #333; /* Changed text color */
            font-weight: 500;
        }

        .card p strong {
            font-weight: 600;
        }

        .img-fluid {
            max-height: 600px; /* Adjusted max-height */
            object-fit: cover;
            border-radius: 20px; /* Adjusted border-radius */
        }

        .btn-warning {
            width: 200px; /* Adjusted width */
            margin-top: 20px; /* Adjusted margin-top */
            font-size: 1.8rem; /* Adjusted font size */
            font-weight: 600;
            padding: 15px; /* Adjusted padding */
            background-color: #f1c40f;
            border: none;
            border-radius: 15px; /* Adjusted border-radius */
        }

        .btn-primary, .btn-danger {
            width: 200px; /* Adjusted width */
            margin-top: 20px; /* Adjusted margin-top */
            font-size: 1.8rem; /* Adjusted font size */
            font-weight: 600;
            padding: 15px; /* Adjusted padding */
            border: none;
            border-radius: 15px; /* Adjusted border-radius */
        }

        .btn-primary {
            background-color: #007bff;
        }

        .btn-danger {
            background-color: #dc3545;
        }

        @media (max-width: 768px) {
            .row {
                flex-direction: column;
                align-items: center;
            }

            .img-fluid {
                max-width: 100%;
                margin-bottom: 15px; /* Adjusted margin-bottom */
            }

            .card {
                width: 100%;
            }

            .btn-warning, .btn-primary, .btn-danger {
                width: 100%;
                margin-top: 10px; /* Adjusted margin-top */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-6 mb-4 mb-md-0">
                <img src="<%= movie.movie_image %>" class="img-fluid" alt="<%= movie.movie_name %>">
            </div>
            <div class="card col-md-6">
                <h1 class="my-4"><%= movie.movie_name %></h1>
                <p><strong>Description:</strong> <%= movie.description %></p>
                <p><strong>Price:</strong> $<%= movie.price %></p>
                <div class="d-flex flex-wrap justify-content-center">
                    <a href="/" class="btn btn-warning m-2">Back to Movies</a>
                    <a href="/update/<%= movie._id %>" class="btn btn-primary m-2">Update</a>
                    <a href="/delete/<%= movie._id %>" class="btn btn-danger m-2">Delete</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
