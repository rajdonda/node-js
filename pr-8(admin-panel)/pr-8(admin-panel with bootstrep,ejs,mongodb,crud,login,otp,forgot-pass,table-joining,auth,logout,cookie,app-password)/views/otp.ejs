<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>OTP Verification</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #1e1e2f, #3a3a60);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      font-family: Arial, sans-serif;
    }
    .otp-container {
      background: rgba(255, 255, 255, 0.1);
      padding: 40px;
      border-radius: 10px;
      backdrop-filter: blur(10px);
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
      width: 100%;
      max-width: 350px;
      text-align: center;
    }
    .otp-container h4 {
      font-weight: bold;
      margin-bottom: 20px;
    }
    .form-control {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: white;
      text-align: center;
    }
    .form-control::placeholder {
      color: rgba(255, 255, 255, 0.7);
    }
    .btn-primary {
      background-color: #ff7b54;
      border: none;
    }
    .btn-primary:hover {
      background-color: #ff5733;
    }
    .form-check-label, a {
      color: rgba(255, 255, 255, 0.8);
    }
  </style>
</head>
<body>
  <div class="otp-container">
    <h4>OTP Verification</h4>
    <p>Enter the OTP sent to your email</p>
    <form action="/verifyotp" method="post">
      <div class="mb-3">
        <label for="otpInput" class="form-label">OTP</label>
        <input type="text" class="form-control" name="otp" id="otpInput" maxlength="6" placeholder="Enter 6-digit OTP" required>
      </div>
      <button type="submit" class="btn btn-primary w-100 py-2">Verify OTP</button>
    </form>
    <div class="mt-3">
      <p>Didn't receive the OTP? <a href="/resendotp" class="text-primary fw-bold">Resend OTP</a></p>
      <a href="/forgot-password" class="text-primary">Back to Forgot Password</a>
    </div>
  </div>
</body>
</html>
